//data types ==> undefined, null, boolean, string, number, symbol, object  

// Declare a Variable 

var myName = "Manju";  // var keyword is a global scope variable -- that can be accessed outside the function or bracket
myName=27;

let employeName="Employee1"; // let keyword is a local scope variable -- that cannot be accesssed outside the function

const workingHour=8;  // const is same as let but we cannot reasign the values for it, it never change


var a; // declaring a variable
var b=2; // assigning a variable

 a=5// later assigning the value to the variable

console.log(a); // allows to see the output or content in console page in web-bowser


//primitive data types
let age=20;
let name='Ram';
let isMajor=true;

// reference data types

let employeeName=['Ram','Shiva','Vela']; // Array

// object
let employeeDetails = {
    name: 'Ram',
    age: 20,
    isMajor: true
};
console.log(employeeDetails);
console.log(employeeName);